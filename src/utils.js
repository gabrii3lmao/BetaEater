/*const { Events } = require('discord.js');

function testarBot(client) {
    client.once(Events.ClientReady, readyClient => {
        console.log(`Ready! Logged in como ${readyClient.user.tag}`);
    });
}

module.exports = { testarBot };*/
